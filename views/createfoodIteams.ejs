<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add New Food | Zomatoo Delight Kitchen</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">

  <!-- Header -->
  <header class="bg-white shadow p-4 flex justify-between items-center">
    <h1 class="text-2xl font-bold text-blue-700">🍲 Add New Food Item</h1>
    <a href="/shop/profile" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Back to Dashboard</a>
  </header>

  <!-- Form Container -->
  <div class="max-w-3xl mx-auto p-6 mt-6 bg-white rounded-lg shadow-md">
    <form action="/createfood" method="POST" enctype="multipart/form-data" class="space-y-6">
    <% if (locals.error) { %>
       <div class="bg-green-100 text-green-800 px-4 py-3 rounded mb-4 border border-green-300">
    ✅ <%= locals.error %>
  </div>
      <% } %>
      <!-- Food Name -->
      <div>
        <label for="foodName" class="block text-gray-700 font-medium mb-1">Food Name</label>
        <input type="text" id="foodName" name="FoodName" required class="w-full border border-gray-300 rounded-md px-4 py-2" />
      </div>

      <!-- Description -->
      <div>
        <label for="description" class="block text-gray-700 font-medium mb-1">Description</label>
        <textarea id="description" name="Description" rows="3" required class="w-full border border-gray-300 rounded-md px-4 py-2" placeholder="Write a short description..."></textarea>
      </div>

      <!-- Price -->
      <div>
        <label for="price" class="block text-gray-700 font-medium mb-1">Price (₹)</label>
        <input type="number" id="price" name="Price" required min="1" class="w-full border border-gray-300 rounded-md px-4 py-2" />
      </div>

      <!-- Stock -->
      <div>
        <label for="stock" class="block text-gray-700 font-medium mb-1">Stock</label>
        <input type="number" id="stock" name="stock" required min="1" class="w-full border border-gray-300 rounded-md px-4 py-2" />
      </div>

      <!-- Image Upload -->
      <div>
        <label for="foodImage" class="block text-gray-700 font-medium mb-1">Food Image</label>
        <input type="file" id="foodImage" name="FoodImage" accept="image/*" required class="w-full border border-gray-300 rounded-md px-4 py-2" />
        <p class="text-xs text-gray-500 mt-1">Upload a high-quality image of your food item</p>
      </div>

      <!-- Submit Button -->
      <div class="text-right">
        <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded-md hover:bg-blue-700 transition">➕ Add Food</button>
      </div>
    
    </form>
  </div>

</body>
</html>
