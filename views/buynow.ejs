<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Buy Now | Zomato Style</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">

  <!-- Header -->
  <header class="bg-white shadow p-4 flex justify-between items-center">
    <h1 class="text-2xl font-bold text-red-600">🍽️ Foodiez - Buy Now</h1>
    <a href="/cart" class="text-sm text-gray-700 hover:text-red-500">← Back to Cart</a>
  </header>

  <!-- Main Container -->
  <div class="max-w-4xl mx-auto mt-8 p-6 bg-white rounded-xl shadow-md">
    
    <!-- Product Section -->
    <div class="flex flex-col md:flex-row gap-6">
      <!-- Food Image -->
      <div class="md:w-1/3">
        <img src="https://source.unsplash.com/featured/?food" alt="Food Item" class="rounded-lg shadow-md object-cover w-full h-52">
      </div>
      <!-- Food Info -->
      <div class="md:w-2/3">
        <h2 class="text-2xl font-bold text-gray-800 mb-2"><%= food.FoodName || "Paneer Butter Masala" %></h2>
        <p class="text-gray-600 mb-2">Delicious and spicy North Indian curry with soft paneer chunks. Prepared fresh!</p>
        <p class="text-xl text-red-500 font-semibold mb-1">₹ <%= food.Price || 220 %></p>
        <p class="text-sm text-gray-500">In Stock: <%= food.Stock || 10 %> items</p>
      </div>
    </div>

    <!-- Divider -->
    <hr class="my-6 border-t border-gray-200" />

    <!-- Delivery Address -->
    <form action="/order/place" method="POST" class="space-y-6">
      <input type="hidden" name="foodId" value="<%= food._id %>" />
      
      <div>
        <label for="address" class="block text-gray-700 font-medium mb-2">Delivery Address</label>
        <textarea name="address" id="address" rows="4" required class="w-full border border-gray-300 rounded-md px-4 py-2 focus:outline-none focus:ring-2 focus:ring-red-300"></textarea>
      </div>

      <!-- Payment Method -->
      <div>
        <label class="block text-gray-700 font-medium mb-2">Payment Method</label>
        <div class="flex items-center space-x-3">
          <input type="radio" id="cod" name="paymentMethod" value="CashOnDelivery" checked class="text-red-600" />
          <label for="cod" class="text-gray-700">Cash on Delivery</label>
        </div>
      </div>

      <!-- Place Order Button -->
      <div class="text-right">
        <button type="submit" class="bg-red-600 text-white px-6 py-2 rounded-md hover:bg-red-700 transition">
          🛒 Place Order Now
        </button>
      </div>
    </form>

  </div>
</body>
</html>
